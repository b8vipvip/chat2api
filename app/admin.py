from __future__ import annotations

from fastapi.responses import HTMLResponse


ADMIN_HTML = r'''<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>chat2api Console</title>
<style>
:root{color-scheme:dark;--bg:#09101d;--panel:#111a2b;--panel2:#0c1526;--line:#26344d;--text:#eef4ff;--muted:#8c9ab2;--accent:#72a7ff;--ok:#39d6a1;--warn:#ffd166;--bad:#ff727d}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:14px/1.55 system-ui,-apple-system,"Segoe UI",sans-serif}.shell{display:grid;grid-template-columns:220px 1fr;min-height:100vh}.side{border-right:1px solid var(--line);padding:20px 14px;background:#0b1322}.brand{font-size:22px;font-weight:850;padding:0 10px 18px}.brand small{display:block;color:var(--muted);font-size:11px}.nav button{display:block;width:100%;border:0;background:transparent;color:var(--muted);text-align:left;padding:10px 12px;border-radius:9px;cursor:pointer;margin:2px 0}.nav button.active,.nav button:hover{background:#17233a;color:var(--text)}.top{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:15px 22px;border-bottom:1px solid var(--line);position:sticky;top:0;background:rgba(9,16,29,.94);z-index:5}.auth{display:flex;gap:8px;min-width:min(100%,560px)}input,select,button,textarea{font:inherit}.field,select,input,textarea,button.action{border:1px solid var(--line);background:var(--panel2);color:var(--text);border-radius:9px;padding:9px 11px}.auth input{flex:1}.action{cursor:pointer;background:#18345e!important}.action.good{background:#154634!important}.action.warn{background:#4b3d15!important}.action.danger{background:#4d2028!important}.main{min-width:0}.content{padding:22px;max-width:1600px;margin:auto}.view{display:none}.view.active{display:block}.muted{color:var(--muted)}.ok{color:var(--ok)}.warnText{color:var(--warn)}.bad{color:var(--bad)}.grid{display:grid;grid-template-columns:repeat(5,minmax(130px,1fr));gap:12px;margin-bottom:14px}.card,.panel{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:15px}.card .n{font-size:25px;font-weight:850}.panels{display:grid;grid-template-columns:1fr 1fr;gap:14px}.wide{grid-column:1/-1}.scroll{overflow:auto;max-height:590px}table{width:100%;border-collapse:collapse;white-space:nowrap}th,td{text-align:left;border-bottom:1px solid var(--line);padding:8px 9px;vertical-align:top}th{color:var(--muted);font-weight:650;position:sticky;top:0;background:var(--panel)}code,pre{font-family:ui-monospace,SFMono-Regular,Consolas,monospace}code{color:#c8d9ff}.pill{display:inline-block;padding:2px 7px;border-radius:999px;background:#1a2943}.pill.ok{background:#123b30}.pill.bad{background:#48242c}.pill.warn{background:#493b17}.toolbar{display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin-bottom:12px}.toolbar>*{min-width:120px}.secret{border:1px solid var(--ok);background:#07101e;padding:12px;border-radius:10px;word-break:break-all}.rowactions{display:flex;gap:6px}.rowactions button{padding:5px 7px}.split{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(330px,.75fr);gap:14px}.detail pre{white-space:pre-wrap;word-break:break-word;font-size:12px}.codebox{background:#07101e;border:1px solid var(--line);border-radius:10px;padding:12px;white-space:pre-wrap;overflow:auto}.doc h2{margin-top:26px}.doc p,.doc li{color:#cbd5e7}.labConfig{display:grid;grid-template-columns:180px 200px minmax(330px,1.2fr) minmax(260px,1fr) auto;gap:9px;align-items:end}.labConfig label{display:block;color:var(--muted);font-size:12px;margin-bottom:5px}.testKeyBox{display:grid;grid-template-columns:1fr 1.25fr;gap:7px}.testKeyHint{grid-column:1/-1;font-size:11px;color:var(--muted);margin-top:-2px}.testStatus{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0}.timeline{min-height:70px}.timelineRow{display:grid;grid-template-columns:130px 130px 1fr 100px 100px 100px;gap:8px;padding:8px;border-bottom:1px solid var(--line);align-items:center}.clickable{cursor:pointer}.clickable:hover{background:#172137}.hidden{display:none!important}.footer{margin-top:18px;color:var(--muted)}dialog{width:min(980px,92vw);max-height:86vh;border:1px solid var(--line);border-radius:14px;background:var(--panel);color:var(--text);padding:0;box-shadow:0 28px 90px rgba(0,0,0,.55)}dialog::backdrop{background:rgba(0,0,0,.66)}.dialogHead,.dialogFoot{display:flex;justify-content:space-between;align-items:center;gap:10px;padding:14px 16px;border-bottom:1px solid var(--line)}.dialogFoot{border-top:1px solid var(--line);border-bottom:0;justify-content:flex-end}.dialogBody{padding:16px;overflow:auto;max-height:65vh}.dialogBody pre{white-space:pre-wrap;word-break:break-word;margin:0;font-size:12px}.keyBadge{max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
@media(max-width:1250px){.labConfig{grid-template-columns:1fr 1fr}.labConfig>button{align-self:end}.testKeyBox{grid-template-columns:1fr}}
@media(max-width:1050px){.shell{grid-template-columns:1fr}.side{border-right:0;border-bottom:1px solid var(--line)}.nav{display:flex;overflow:auto}.nav button{width:auto;white-space:nowrap}.top{position:static}.grid{grid-template-columns:repeat(2,1fr)}.panels,.split{grid-template-columns:1fr}.wide{grid-column:auto}.labConfig{grid-template-columns:1fr}.timelineRow{grid-template-columns:1fr 1fr}.auth{min-width:0}.testKeyBox{grid-template-columns:1fr}}
</style></head><body><div class="shell">
<aside class="side"><div class="brand">chat2api<small>Server Console · v0.5</small></div><div class="nav"><button data-view="overview">概览</button><button data-view="keys">API Key</button><button data-view="requests">请求记录</button><button data-view="docs">开发文档</button><button data-view="playground">测试场</button></div></aside>
<main class="main"><header class="top"><div><b id="pageTitle">概览</b><div id="status" class="muted">输入管理员主密钥后连接</div></div><div class="auth"><input id="adminKey" type="password" placeholder="CHAT2API_API_KEY 管理员主密钥"><button class="action" id="connect">连接</button><button class="action" id="refresh">刷新</button></div></header><div class="content">
<section class="view" id="view-overview"><div class="grid"><div class="card"><div class="muted">在线扩展</div><div class="n" id="sExt">-</div></div><div class="card"><div class="muted">请求记录</div><div class="n" id="sReq">-</div></div><div class="card"><div class="muted">成功率</div><div class="n" id="sSuccess">-</div></div><div class="card"><div class="muted">平均首 Token</div><div class="n" id="sFirst">-</div></div><div class="card"><div class="muted">估算 Token</div><div class="n" id="sToken">-</div></div></div><div class="panels"><div class="panel"><h3>Chrome 扩展</h3><table><thead><tr><th>ID</th><th>名称</th><th>状态</th><th>版本</th><th>能力</th></tr></thead><tbody id="extBody"></tbody></table></div><div class="panel"><h3>模型目录</h3><table><thead><tr><th>ID</th><th>能力</th></tr></thead><tbody id="modelBody"></tbody></table></div><div class="panel wide"><h3>最近请求</h3><div class="scroll"><table><thead><tr><th>时间</th><th>类型</th><th>状态</th><th>Key</th><th>模型</th><th>首包</th><th>总耗时</th><th>Token</th></tr></thead><tbody id="recentBody"></tbody></table></div></div></div></section>
<section class="view" id="view-keys"><div class="panel"><h3>创建业务 API Key</h3><div class="toolbar"><input id="keyName" placeholder="名称"><input id="keyDays" type="number" min="1" max="3650" placeholder="有效天数"><button class="action good" id="createKey">创建</button></div><div class="secret hidden" id="newSecret"><span id="newToken"></span> <button class="action" id="copyNew">复制</button></div></div><div class="panel" style="margin-top:14px"><h3>令牌列表</h3><div class="scroll"><table><thead><tr><th>名称</th><th>前缀</th><th>状态</th><th>权限</th><th>最后使用</th><th>请求/Token</th><th>操作</th></tr></thead><tbody id="keysBody"></tbody></table></div></div><div class="footer">新版本业务 Key 的可复制副本使用管理员主密钥派生密钥加密保存；鉴权仍只比较哈希。旧版 0.4 Key 没有密文副本，因此无法恢复。</div></section>
<section class="view" id="view-requests"><div class="split"><div class="panel"><h3>请求记录</h3><div class="toolbar"><input id="rqSearch" placeholder="request / error / client"><select id="rqStatus"><option value="">全部状态</option><option>completed</option><option>error</option></select><input id="rqModel" placeholder="模型"><button class="action" id="rqGo">筛选</button></div><div class="scroll"><table><thead><tr><th>时间</th><th>类型</th><th>状态</th><th>Key</th><th>模型</th><th>附件</th><th>首包</th><th>总耗时</th><th>Token</th></tr></thead><tbody id="rqBody"></tbody></table></div></div><div class="panel detail"><h3>请求详情</h3><div id="rqDetail" class="muted">点击记录查看诊断。</div></div></div></section>
<section class="view doc" id="view-docs"><div class="panel"><h1>开发文档</h1><p>Base URL：<code id="baseUrl"></code>。业务 Key 支持文本、视觉/文件理解和图片生成；Chrome 必须打开且扩展在线，当前版本不再依赖 Windows 桌面客户端。</p><h2>文本 / 视觉 / 文件理解</h2><div class="codebox" id="docChat"></div><p>先通过 <code>POST /v1/files</code> 上传 base64 文件，再把返回的 <code>file_id</code> 放入 <code>attachments</code>。图片也支持 OpenAI 风格 <code>image_url</code> 的 base64 data URL。</p><h2>上传文件</h2><div class="codebox" id="docFile"></div><h2>图片生成</h2><div class="codebox" id="docImage"></div><p><code>gpt-image</code> 会路由到 <code>https://chatgpt.com/images/</code>。参考图使用同一个 <code>attachments</code> 字段。当前 <code>n</code> 固定为 1，推荐 <code>response_format=b64_json</code>。</p><h2>状态码</h2><ul><li>401：Key 无效/过期/停用。</li><li>409：扩展忙或多个扩展未指定 client_id。</li><li>503：Chrome 扩展离线。当前不会再尝试唤醒桌面客户端。</li><li>504：等待 ChatGPT/Images 超时。</li></ul><p><a href="/docs" target="_blank" style="color:var(--accent)">打开 FastAPI OpenAPI</a></p></div></section>
<section class="view" id="view-playground"><div class="panel"><h2>自动测试场</h2><div class="muted">执行标准化用例并生成质量报告。测试 Key 支持选择现有业务 Key 或手动粘贴；手动粘贴优先，未指定时默认使用管理员 CHAT2API_API_KEY。</div><div class="labConfig" style="margin-top:14px"><div><label>测试类型</label><select id="testType"><option value="text">文本</option><option value="vision">视觉理解</option><option value="file">文件理解</option><option value="image_generation">图片生成</option><option value="voice_generation">语音生成</option><option value="voice_conversation">语音对话</option><option value="all">全部测试</option></select></div><div><label>文本/理解模型</label><select id="testModel"><option value="default">default</option></select></div><div><label>测试 API Key</label><div class="testKeyBox"><select id="testKeySelect"><option value="master">管理员 CHAT2API_API_KEY（默认）</option></select><input id="testKeyInput" type="password" autocomplete="off" placeholder="粘贴 API Key（可选，优先使用）"><div class="testKeyHint">留空时使用左侧选择项；默认选择管理员主 Key。完整 Key 不写入测试报告。</div></div></div><div><label>测试附件（可多选：图片 / PDF / 文档等）</label><input id="testFiles" type="file" multiple></div><button class="action good" id="startTest">开始测试</button></div><div class="testStatus"><div class="card"><div class="muted">当前状态</div><div class="n" id="testState">待命</div></div><div class="card"><div class="muted">通过</div><div class="n" id="testPass">0</div></div><div class="card"><div class="muted">警告</div><div class="n" id="testWarn">0</div></div><div class="card"><div class="muted">失败</div><div class="n" id="testFail">0</div></div></div><div class="panel"><h3>实时监测</h3><div class="timeline" id="timeline"><div class="muted">尚未开始测试。</div></div></div></div><div class="panel" style="margin-top:14px"><h3>测试记录</h3><div class="scroll"><table><thead><tr><th>时间</th><th>类型</th><th>状态</th><th>模型</th><th>测试 Key</th><th>耗时</th><th>摘要</th><th>操作</th></tr></thead><tbody id="testHistory"></tbody></table></div></div></section>
<div class="footer">Token 为 chat2api 本地估算，不是 ChatGPT 官方 usage。语音能力在本版测试场中会标记为未实现/跳过，不伪装为通过。</div></div></main></div>
<dialog id="reportDialog"><div class="dialogHead"><div><b>测试报告</b><div id="reportTitle" class="muted"></div></div><button class="action" id="closeReport">关闭</button></div><div class="dialogBody"><pre id="reportContent"></pre></div><div class="dialogFoot"><button class="action" id="downloadCurrentReport">下载 JSON</button><button class="action" id="closeReportBottom">关闭</button></div></dialog>
<script>
const INITIAL_VIEW='__INITIAL_VIEW__';
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmtMs=v=>v==null?'-':(Number(v)<1000?Math.round(v)+' ms':(Number(v)/1000).toFixed(1)+' s');
const fmtTime=v=>v?String(v).replace('T',' ').slice(0,19):'-';
let overview=null;
let testKeyCatalog=[];
let activeTestReport=null;
function key(){return sessionStorage.getItem('chat2api_admin_key')||''}
function headers(k=key()){return{Authorization:'Bearer '+k}}
async function api(path,opt={}){const h=headers(opt.key||key());if(opt.body!==undefined)h['Content-Type']='application/json';const r=await fetch(path,{method:opt.method||'GET',headers:h,body:opt.body===undefined?undefined:JSON.stringify(opt.body),cache:'no-store'});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch(_){d={detail:t}}if(!r.ok)throw new Error(d.detail||`${r.status} ${t}`);return d}
function pill(v){const c=v==='completed'||v==='passed'||v==='online'?'ok':v==='warning'||v==='skipped'||v==='busy'?'warn':'bad';return`<span class="pill ${c}">${esc(v)}</span>`}
function status(t,c='muted'){$('status').className=c;$('status').textContent=t}
$('adminKey').value=key();
$('connect').onclick=()=>{sessionStorage.setItem('chat2api_admin_key',$('adminKey').value.trim());loadOverview()};
$('refresh').onclick=()=>show((location.hash||'#overview').slice(1));
const titles={overview:'概览',keys:'API Key',requests:'请求记录',docs:'开发文档',playground:'测试场'};
function show(v){document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.nav button').forEach(x=>x.classList.toggle('active',x.dataset.view===v));$('view-'+v)?.classList.add('active');$('pageTitle').textContent=titles[v]||v;location.hash=v;if(v==='overview')loadOverview();if(v==='keys')loadKeys();if(v==='requests')loadRequests();if(v==='docs')renderDocs();if(v==='playground'){loadOverview().then(loadTests)}}
document.querySelectorAll('.nav button').forEach(b=>b.onclick=()=>show(b.dataset.view));
async function loadOverview(){if(!key()){status('请输入管理员主密钥');return}try{overview=await api('/api/admin/overview');status(`已连接 · v${overview.version}`,'ok');const s=overview.telemetry||{};$('sExt').textContent=overview.health?.online_extensions??0;$('sReq').textContent=s.retained_requests??0;$('sSuccess').textContent=(s.success_rate??100)+'%';$('sFirst').textContent=fmtMs(s.avg_first_token_ms);$('sToken').textContent=s.estimated_total_tokens??0;$('extBody').innerHTML=(overview.clients||[]).map(c=>`<tr><td><code>${esc(c.client_id)}</code></td><td>${esc(c.name)}</td><td>${pill(c.online?(c.busy?'busy':'online'):'offline')}</td><td>${esc(c.version)}</td><td>${esc((c.metadata?.capabilities||[]).join(', '))}</td></tr>`).join('')||'<tr><td colspan="5">暂无扩展</td></tr>';$('modelBody').innerHTML=(overview.models||[]).map(m=>`<tr><td><code>${esc(m.id)}</code></td><td>${esc((m.capabilities||[]).join(', '))}</td></tr>`).join('');$('recentBody').innerHTML=(overview.recent_requests||[]).map(r=>`<tr><td>${fmtTime(r.recorded_at)}</td><td>${esc(r.request_type||'text')}</td><td>${pill(r.status)}</td><td>${esc(r.api_key_name||'-')}</td><td><code>${esc(r.requested_model||'-')}</code></td><td>${fmtMs(r.timings?.first_token_ms)}</td><td>${fmtMs(r.timings?.total_ms)}</td><td>${esc(r.usage?.total_tokens??0)}</td></tr>`).join('');fillModels();return overview}catch(e){status('连接失败：'+e.message,'bad')}}
function fillModels(){if(!overview)return;const ids=[...new Set(['default',...(overview.models||[]).filter(m=>m.id!=='gpt-image').map(m=>m.id)])];$('testModel').innerHTML=ids.map(id=>`<option value="${esc(id)}">${esc(id)}</option>`).join('')}
async function loadKeys(){if(!key())return;try{const d=await api('/api/admin/keys');$('keysBody').innerHTML=(d.data||[]).map(k=>{let actions=k.managed&&!k.revoked_at?`<div class="rowactions">${k.secret_recoverable?`<button class="action" onclick="copyKey('${esc(k.key_id)}')">复制 Key</button>`:''}<button class="action" onclick="toggleKey('${esc(k.key_id)}',${!k.enabled})">${k.enabled?'停用':'启用'}</button><button class="action danger" onclick="revokeKey('${esc(k.key_id)}')">撤销</button></div>`:'<span class="muted">.env 管理</span>';return`<tr><td>${esc(k.name)}</td><td><code>${esc(k.prefix)}</code></td><td>${pill(k.revoked_at?'revoked':k.expired?'expired':k.enabled?'online':'disabled')}</td><td>${esc((k.scopes||[]).join(', '))}</td><td>${fmtTime(k.last_used_at)}</td><td>${k.request_count??0} / ${k.estimated_tokens??0}</td><td>${actions}</td></tr>`}).join('')}catch(e){status(e.message,'bad')}}
$('createKey').onclick=async()=>{try{const name=$('keyName').value.trim();if(!name)throw new Error('请输入名称');const days=Number($('keyDays').value||0);const d=await api('/api/admin/keys',{method:'POST',body:{name,expires_in_days:days||null}});$('newToken').textContent=d.token;$('newSecret').classList.remove('hidden');await loadKeys()}catch(e){status(e.message,'bad')}};
$('copyNew').onclick=()=>navigator.clipboard.writeText($('newToken').textContent||'');
async function copyKey(id){try{const d=await api(`/api/admin/keys/${encodeURIComponent(id)}/secret`);await navigator.clipboard.writeText(d.token);status('API Key 已复制','ok')}catch(e){status(e.message,'bad')}}
async function toggleKey(id,enabled){try{await api(`/api/admin/keys/${encodeURIComponent(id)}`,{method:'PATCH',body:{enabled}});loadKeys()}catch(e){status(e.message,'bad')}}
async function revokeKey(id){if(!confirm('确定永久撤销？'))return;try{await api(`/api/admin/keys/${encodeURIComponent(id)}`,{method:'DELETE'});loadKeys()}catch(e){status(e.message,'bad')}}
window.copyKey=copyKey;window.toggleKey=toggleKey;window.revokeKey=revokeKey;
async function loadRequests(){if(!key())return;try{const p=new URLSearchParams({limit:'100'});if($('rqSearch').value.trim())p.set('q',$('rqSearch').value.trim());if($('rqStatus').value)p.set('status',$('rqStatus').value);if($('rqModel').value.trim())p.set('model',$('rqModel').value.trim());const d=await api('/api/admin/requests?'+p);$('rqBody').innerHTML=(d.data||[]).map(r=>`<tr class="clickable" onclick="requestDetail('${esc(r.request_id)}')"><td>${fmtTime(r.recorded_at)}</td><td>${esc(r.request_type||'text')}</td><td>${pill(r.status)}</td><td>${esc(r.api_key_name||'-')}</td><td><code>${esc(r.requested_model)}</code></td><td>${r.attachments_count??0}</td><td>${fmtMs(r.timings?.first_token_ms)}</td><td>${fmtMs(r.timings?.total_ms)}</td><td>${r.usage?.total_tokens??0}</td></tr>`).join('')}catch(e){status(e.message,'bad')}}
$('rqGo').onclick=loadRequests;
async function requestDetail(id){try{const d=await api('/api/admin/requests/'+encodeURIComponent(id));$('rqDetail').innerHTML=`<b>${esc(d.request_id)}</b><pre>${esc(JSON.stringify(d,null,2))}</pre>`}catch(e){status(e.message,'bad')}}
window.requestDetail=requestDetail;
function renderDocs(){const b=location.origin;$('baseUrl').textContent=b+'/v1';$('docChat').textContent=`POST ${b}/v1/chat/completions\nAuthorization: Bearer YOUR_API_KEY\n\n{\n  "model":"default",\n  "messages":[{"role":"user","content":"请分析附件"}],\n  "attachments":[{"file_id":"file_xxx"}],\n  "stream":true\n}`;$('docFile').textContent=`POST ${b}/v1/files\n{\n  "filename":"photo.png",\n  "mime_type":"image/png",\n  "data_base64":"iVBOR...",\n  "purpose":"vision"\n}`;$('docImage').textContent=`POST ${b}/v1/images/generations\n{\n  "model":"gpt-image",\n  "prompt":"画一只在月光下的狐狸",\n  "response_format":"b64_json",\n  "attachments":[{"file_id":"file_optional_reference"}]\n}`}
function fileToBase64(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result).split(',')[1]||'');r.onerror=reject;r.readAsDataURL(file)})}
async function loadTestKeys(){const current=$('testKeySelect').value||'master';const d=await api('/api/admin/keys');testKeyCatalog=d.data||[];const options=['<option value="master">管理员 CHAT2API_API_KEY（默认）</option>'];for(const item of testKeyCatalog){if(!item.managed)continue;const usable=item.enabled&&!item.expired&&!item.revoked_at&&item.secret_recoverable;const state=item.revoked_at?'已撤销':item.expired?'已过期':!item.enabled?'已停用':!item.secret_recoverable?'旧 Key 无法恢复':'可用';options.push(`<option value="${esc(item.key_id)}" ${usable?'':'disabled'}>${esc(item.name)} · ${esc(item.prefix)} · ${esc(state)}</option>`)}$('testKeySelect').innerHTML=options.join('');if([...$('testKeySelect').options].some(o=>o.value===current&&!o.disabled))$('testKeySelect').value=current}
function maskKey(value){const v=String(value||'').trim();if(v.length<=12)return v?`${v.slice(0,4)}…`:'-';return`${v.slice(0,8)}…${v.slice(-4)}`}
async function resolveTestCredential(){const pasted=$('testKeyInput').value.trim();if(pasted)return{token:pasted,label:`手动粘贴 ${maskKey(pasted)}`,source:'pasted',key_id:null};const selected=$('testKeySelect').value||'master';if(selected==='master')return{token:key(),label:'管理员 CHAT2API_API_KEY',source:'admin',key_id:'master'};const item=testKeyCatalog.find(x=>x.key_id===selected);if(!item)throw new Error('所选测试 Key 不存在，请刷新后重试');if(!item.secret_recoverable)throw new Error('所选旧版 Key 没有可恢复密文，请手动粘贴完整 Key');const d=await api(`/api/admin/keys/${encodeURIComponent(selected)}/secret`);return{token:d.token,label:`${item.name} · ${item.prefix}`,source:'managed',key_id:selected}}
async function upload(file,purpose,testToken){const b64=await fileToBase64(file);return api('/v1/files',{method:'POST',key:testToken,body:{filename:file.name,mime_type:file.type||'application/octet-stream',data_base64:b64,purpose}})}
async function streamChat(model,prompt,attachments=[],testToken=key()){const started=performance.now();let first=null,totalText=0,meta=null,rawError='';const r=await fetch('/v1/chat/completions',{method:'POST',headers:{...headers(testToken),'Content-Type':'application/json'},body:JSON.stringify({model,messages:[{role:'user',content:prompt}],attachments,stream:true,timeout:300})});if(!r.ok)throw new Error(`${r.status} ${await r.text()}`);const reader=r.body.getReader(),dec=new TextDecoder();let buf='';while(true){const {done,value}=await reader.read();if(done)break;buf+=dec.decode(value,{stream:true});let i;while((i=buf.indexOf('\n\n'))>=0){const block=buf.slice(0,i);buf=buf.slice(i+2);for(const line of block.split('\n')){if(!line.startsWith('data: '))continue;const payload=line.slice(6);if(payload==='[DONE]')continue;let d;try{d=JSON.parse(payload)}catch(_){continue}if(d.error){rawError=d.error.message||'stream error';continue}const c=d.choices?.[0]?.delta?.content||'';if(c){if(first===null)first=performance.now()-started;totalText+=c.length}if(d.usage||d.chat2api)meta={usage:d.usage,chat2api:d.chat2api}}}}if(rawError)throw new Error(rawError);return{first_token_ms:first,total_ms:performance.now()-started,response_chars:totalText,meta}}
function quality(result){if(result.status==='failed')return{grade:'failed',issues:['调用失败：'+(result.error||'unknown')]};if(result.status==='skipped')return{grade:'skipped',issues:[result.reason||'未实现']};const issues=[];if(result.first_token_ms!=null&&result.first_token_ms>30000)issues.push('首包延迟超过 30 秒');if(result.total_ms!=null&&result.total_ms>90000)issues.push('总耗时超过 90 秒');if(result.kind!=='image_generation'&&result.response_chars===0)issues.push('没有捕获到文本输出');if(!result.meta?.chat2api&&result.kind!=='image_generation')issues.push('缺少 chat2api diagnostics');return{grade:issues.length?'warning':'passed',issues}}
function row(r){return`<div class="timelineRow"><b>${esc(r.label)}</b><span>${pill(r.status)}</span><span>${esc(r.message||'')}</span><span>${fmtMs(r.first_token_ms)}</span><span>${fmtMs(r.total_ms)}</span><span>${esc(r.quality?.grade||'-')}</span></div>`}
function redraw(rows){$('timeline').innerHTML='<div class="timelineRow muted"><b>项目</b><span>状态</span><span>诊断</span><span>首包</span><span>总耗时</span><span>质量</span></div>'+rows.map(row).join('');$('testPass').textContent=rows.filter(x=>x.status==='passed').length;$('testWarn').textContent=rows.filter(x=>x.status==='warning'||x.status==='skipped').length;$('testFail').textContent=rows.filter(x=>x.status==='failed').length}
async function oneTest(kind,model,files,testToken){const labels={text:'文本',vision:'视觉理解',file:'文件理解',image_generation:'图片生成',voice_generation:'语音生成',voice_conversation:'语音对话'};const r={kind,label:labels[kind]||kind,status:'running',message:'执行中'};const started=performance.now();try{if(kind.startsWith('voice_')){r.status='skipped';r.reason='当前版本尚未实现语音桥接';r.message=r.reason;return r}if(kind==='text'){Object.assign(r,await streamChat(model,'请只回复：chat2api 文本测试成功',[],testToken));}else if(kind==='vision'){const f=files.find(x=>x.type.startsWith('image/'));if(!f){r.status='skipped';r.reason='未选择图片附件';r.message=r.reason;return r}const u=await upload(f,'vision',testToken);Object.assign(r,await streamChat(model,'请识别附件图片，并用一句话描述主要内容。',[{file_id:u.id}],testToken));}else if(kind==='file'){const f=files.find(x=>!x.type.startsWith('image/'));if(!f){r.status='skipped';r.reason='未选择文档/文件附件';r.message=r.reason;return r}const u=await upload(f,'file-understanding',testToken);Object.assign(r,await streamChat(model,'请阅读附件，并用一句话概括核心内容。',[{file_id:u.id}],testToken));}else if(kind==='image_generation'){const image=files.find(x=>x.type.startsWith('image/'));const at=[];if(image){const u=await upload(image,'image-reference',testToken);at.push({file_id:u.id})}const t=performance.now();const d=await api('/v1/images/generations',{method:'POST',key:testToken,body:{model:'gpt-image',prompt:image?'参考附件图片，生成一张风格明显不同但主体相关的新图片。':'生成一张简洁的蓝天白云测试图片。',response_format:'b64_json',attachments:at,timeout:600}});r.total_ms=performance.now()-t;r.image_count=d.data?.length||0;r.meta={usage:d.usage,chat2api:d.chat2api};if(!r.image_count)throw new Error('图片接口没有返回图片数据');}r.status='passed';r.message='调用完成'}catch(e){r.status='failed';r.error=String(e.message||e);r.message=r.error}finally{r.total_ms=r.total_ms??(performance.now()-started);r.quality=quality(r);if(r.status==='passed'&&r.quality.grade==='warning')r.status='warning'}return r}
$('startTest').onclick=async()=>{if(!key())return status('请先连接管理员主密钥','bad');let credential;try{credential=await resolveTestCredential()}catch(e){return status('测试 Key 读取失败：'+e.message,'bad')}const type=$('testType').value,model=$('testModel').value,files=[...$('testFiles').files],kinds=type==='all'?['text','vision','file','image_generation','voice_generation','voice_conversation']:[type];const rows=[];$('testState').textContent='运行中';redraw(rows);status(`正在使用：${credential.label}`,'ok');const startedAt=new Date().toISOString(),start=performance.now();for(const kind of kinds){const pending={kind,label:{text:'文本',vision:'视觉理解',file:'文件理解',image_generation:'图片生成',voice_generation:'语音生成',voice_conversation:'语音对话'}[kind],status:'running',message:'执行中'};rows.push(pending);redraw(rows);const done=await oneTest(kind,model,files,credential.token);rows[rows.length-1]=done;redraw(rows)}const failed=rows.some(x=>x.status==='failed'),warn=rows.some(x=>x.status==='warning'),passed=rows.some(x=>x.status==='passed');const reportStatus=failed?'failed':warn?'warning':passed?'passed':'skipped';const runId='testrun_'+Date.now().toString(36)+Math.random().toString(36).slice(2,7);const report={run_id:runId,test_type:type,status:reportStatus,model,started_at:startedAt,finished_at:new Date().toISOString(),duration_ms:performance.now()-start,summary:`${rows.filter(x=>x.status==='passed').length} passed, ${rows.filter(x=>x.status==='warning').length} warning, ${rows.filter(x=>x.status==='failed').length} failed, ${rows.filter(x=>x.status==='skipped').length} skipped`,results:rows,quality:{api_key_label:credential.label,api_key_source:credential.source,api_key_id:credential.key_id,bugs:rows.filter(x=>x.status==='failed').map(x=>x.message),latency_warnings:rows.flatMap(x=>x.quality?.issues||[]).filter(x=>/延迟|耗时/.test(x))}};try{await api('/api/admin/tests',{method:'POST',body:report});$('testState').textContent=reportStatus;await loadTests()}catch(e){status('保存测试报告失败：'+e.message,'bad')}};
async function loadTests(){if(!key())return;try{await loadTestKeys();const d=await api('/api/admin/tests?limit=50');$('testHistory').innerHTML=(d.data||[]).map(r=>`<tr><td>${fmtTime(r.recorded_at||r.finished_at)}</td><td>${esc(r.test_type)}</td><td>${pill(r.status)}</td><td>${esc(r.model||'-')}</td><td class="keyBadge" title="${esc(r.quality?.api_key_label||'-')}">${esc(r.quality?.api_key_label||'-')}</td><td>${fmtMs(r.duration_ms)}</td><td>${esc(r.summary||'')}</td><td><div class="rowactions"><button class="action" onclick="showTest('${esc(r.run_id)}')">查看报告</button><button class="action" onclick="downloadTest('${esc(r.run_id)}')">下载</button></div></td></tr>`).join('')||'<tr><td colspan="8" class="muted">暂无测试记录</td></tr>'}catch(e){status(e.message,'bad')}}
async function fetchTestReport(id){return api('/api/admin/tests/'+encodeURIComponent(id))}
async function showTest(id){try{const d=await fetchTestReport(id);activeTestReport=d;$('reportTitle').textContent=`${d.run_id} · ${d.test_type} · ${d.status}`;$('reportContent').textContent=JSON.stringify(d,null,2);$('reportDialog').showModal()}catch(e){status(e.message,'bad')}}
function downloadReportObject(d){const blob=new Blob([JSON.stringify(d,null,2)],{type:'application/json;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`${d.run_id||'chat2api-test-report'}.json`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
async function downloadTest(id){try{downloadReportObject(await fetchTestReport(id))}catch(e){status(e.message,'bad')}}
$('closeReport').onclick=()=>$('reportDialog').close();
$('closeReportBottom').onclick=()=>$('reportDialog').close();
$('downloadCurrentReport').onclick=()=>{if(activeTestReport)downloadReportObject(activeTestReport)};
window.showTest=showTest;window.downloadTest=downloadTest;
const start=(location.hash||'#'+INITIAL_VIEW).slice(1);show(titles[start]?start:'overview');
</script></body></html>'''


def admin_response(initial_view: str = "overview") -> HTMLResponse:
    view = initial_view if initial_view in {"overview", "keys", "requests", "docs", "playground"} else "overview"
    return HTMLResponse(ADMIN_HTML.replace("__INITIAL_VIEW__", view), headers={"Cache-Control": "no-store"})
