services:
  chat2api:
    build: .
    restart: unless-stopped
    stop_grace_period: 20s
    env_file: .env
    ports:
      - "${CHAT2API_PORT:-8765}:${CHAT2API_PORT:-8765}"
    volumes:
      - ./data:/app/data
