# 已更名为 GitHub Agent

原 **GitHub Actions 策略** 已正式更名为 **GitHub Agent**，当前仓库执行 **GitHub Agent v4**。

新的规范入口：[`GITHUB_AGENT.md`](./GITHUB_AGENT.md)

本文件仅用于兼容历史链接和旧自动化引用。现有 `actions-*` / `actions_strategy_*` 文件名继续作为内部兼容名称，不代表仍使用 v3 策略。
