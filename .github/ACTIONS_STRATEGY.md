# 已更名为 GitHub Agent

原 **GitHub Actions 策略 v3** 已正式更名为 **GitHub Agent v3**。

新的规范入口：[`GITHUB_AGENT.md`](./GITHUB_AGENT.md)

本文件暂时保留，仅用于兼容历史链接和旧自动化引用。现有 `actions-*` / `actions_strategy_*` 文件名也作为内部兼容名称继续保留，不影响 GitHub Agent 的实际运行。
